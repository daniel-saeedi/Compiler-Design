class B {
    private int x;

    public initialize (int x) {
        self.x = x;
    }
}

class A {
    private int x;
    private B instance;
    public initialize () {

        self.x = 3 ? true : 5;
        self.x = self.apply();
        self.instance = B.new();
    }
    private int apply(int x, int y, int k = 1) {
        return
    }
}
